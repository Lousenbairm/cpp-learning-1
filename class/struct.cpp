#include <iostream>
#include <string.h>

#define LOG(x) std::cout << x << std::endl;

int main() {

    //struct is public in default, class is private in default. Everything is the same for both
    std::cin.get();
}